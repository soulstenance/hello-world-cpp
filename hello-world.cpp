#include <iostream>

// We declare the std namespace now, in lieu of declaring it repeatedly throughout the code.
using namespace std;

int main() {
    cout << "Hello World!" << endl << "We are in the already declared 'std' namespace!" << endl;
    return 0;
}
